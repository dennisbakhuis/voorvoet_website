# Cloudflare Turnstile Configuration
# Get your site key from: https://dash.cloudflare.com/?to=/:account/turnstile
TURNSTILE_SITE_KEY=your_site_key_here

# Turnstile Secret Key (for server-side verification)
# Keep this secret! Never commit this to version control
TURNSTILE_SECRET_KEY=your_secret_key_here

# Enable/disable Turnstile bot protection (true/false)
# Set to false for local development to bypass bot verification
TURNSTILE_ENABLED=true

# Turnstile Dummy Mode (for testing when no real keys are set)
# Options: always_pass, always_fail, always_pass_invisible, always_fail_invisible, interactive
# always_pass: Visible widget that always passes (test successful submissions)
# always_fail: Visible widget that always fails (test error handling)
# always_pass_invisible: Invisible widget that always passes (test invisible success)
# always_fail_invisible: Invisible widget that always fails (test invisible errors)
# interactive: Forces interactive challenge (test user interaction)
TURNSTILE_DUMMY_MODE=always_pass

# SMTP Configuration (Proton Mail)
# SMTP server settings for sending contact form emails
SMTP_HOST=smtp.protonmail.ch
SMTP_PORT=587
SMTP_USERNAME=your_proton_email@proton.me
SMTP_PASSWORD=your_proton_app_password
SMTP_FROM_EMAIL=your_proton_email@proton.me
SMTP_TO_EMAIL=your_notification_email@example.com

# Link to appointment portal
LINK_PLAN_PORTAL=https://your_url_to_plan_appointments.nl

# Blog Settings
# Control what metadata is displayed on blog posts
BLOG_SHOW_AUTHOR=false
BLOG_SHOW_PUBLICATION_DATE=false

# Site URL
# Base URL of the website (used for Open Graph and canonical URLs)
SITE_URL=https://voorvoet.nl

# Reimbursements Data Configuration
# Specify which data files to use for the reimbursements page
# Files must be located in voorvoet_website/data/reimbursements/
REIMBURSEMENTS_DATA_FILE=reimbursements_2025.json
PRICING_DATA_FILE=pricing_2025.csv

# Umami Analytics Configuration
# Leave empty to disable Umami analytics tracking
UMAMI_SCRIPT_URL=
UMAMI_WEBSITE_ID=
